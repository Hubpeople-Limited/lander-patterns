# features@1

Generated by `ci/compose.py` from `ci/page-recipes.json` - never hand-edit; change the source and regenerate.

A **features** page shell. What the brand actually does, as a page: the claim, the feature panels climbing the ground ladder, a table comparing what each tier includes, and the questions that follow. The page a partner asks for when the homepage cannot carry every capability.

Fetch `page.html` and `page.css`, write real copy into the slots, duplicate whatever block a pattern's comments say to duplicate, strip every comment, and keep each `{{ }}` token exactly as found - the platform fills those.

## Wiring

The brand's stylesheet loads first: link the brand's own `global.css` - its token values, per [TOKENS.md](../../TOKENS.md) - ahead of `page.css`, or merge `page.css` into it unchanged. `page.css` carries only the patterns' rules and reads every colour, radius and font from the brand's tokens.

The page carries its own site footer: this shell closes with the `colophon` pattern, and the platform fills its legal links and footer menu at serve time - keep every `{{ }}` token in it exactly as found. A site header is likewise a pattern a page carries (`masthead-nav` in this library); this composition ships without one.

## Sections, in order

1. **masthead-nav** v8 - The site header the platform menu placeholder has no styling for - brand mark, the generated primary navigation and the login and join controls, on a choice of ground, two desktop arrangements, three small-screen behaviours, an optional pinned bar, two small-screen alignments, a menu button with or without its word, and three bottom edges.
   - `home-url` - attribute (href): a short label, about 1 word(s)
   - `menu-label` - text, about 2 word(s). Sample: Menu (sample)
   - `close-label` - text, about 2 word(s). Sample: Close (sample)
2. **hero-stated** v4 (ground=plain) - An opener made of words - a display claim, one supporting line and the join control - on a choice of three grounds and two alignments, with no photograph anywhere in it.
   - `eyebrow` - text, about 2 word(s). Sample: Sample eyebrow
   - `headline` - text, about 7 word(s). Sample: A sample headline for preview, set large
   - `subhead` - text, about 9 word(s). Sample: One supporting sentence, used only to render this preview.
   - `assurance` - markup, about 4 words of copy; uses cta-assurance, whose CSS is in page.css; its sample shape is printed in page.html's banner
3. **feature-panels** v5 - A run of full-bleed panels climbing a ground ladder - light, then the brand colour, then dark - each carrying a mark, a label, one large claim and a link on its floor.
   - `section-id` - attribute (aria-labelledby/id): a page-unique id drawn from the heading
   - `eyebrow` - text, about 2 word(s). Sample: Sample eyebrow
   - `section-title` - text, about 3 word(s). Sample: Sample panels heading
   - `section-intro` - text, about 9 word(s). Sample: One supporting sentence, used only to render this preview.
   - `panel-1-label` - text, about 2 word(s). Sample: Sample one
   - `panel-1-claim` - text, about 6 word(s). Sample: Sample claim for the first panel
   - `panel-1-link-text` - text, about 2 word(s). Sample: Sample link
   - `panel-2-label` - text, about 2 word(s). Sample: Sample two
   - `panel-2-claim` - text, about 6 word(s). Sample: Sample claim for the second panel
   - `panel-2-link-text` - text, about 2 word(s). Sample: Sample link
   - `panel-3-label` - text, about 2 word(s). Sample: Sample three
   - `panel-3-claim` - text, about 6 word(s). Sample: Sample claim for the third panel
   - `panel-3-link-text` - text, about 2 word(s). Sample: Sample link
4. **comparison-table** v4 - A real table comparing three things against the same criteria - tier names across the top, one criterion per row, the recommended column named in words and never by colour alone.
   - `section-id` - attribute (aria-labelledby/id): a page-unique id drawn from the heading
   - `section-title` - text, about 3 word(s). Sample: Sample comparison heading
   - `section-intro` - text, about 9 word(s). Sample: One supporting sentence, used only to render this preview.
   - `table-label` - attribute (aria-label): a short label, about 4 word(s)
   - `table-caption` - text, about 7 word(s). Sample: A sample caption for the preview render.
   - `tier-a` - text, about 2 word(s). Sample: Sample A
   - `tier-b` - text, about 2 word(s). Sample: Sample B
   - `tier-b-flag` - text, about 2 word(s). Sample: Sample flag
   - `tier-c` - text, about 2 word(s). Sample: Sample C
   - `criterion` - text, about 2 word(s). Sample: Sample criterion
   - `cell-a` - text, about 2 word(s). Sample: Sample value
   - `cell-b` - text, about 4 word(s). Sample: Sample value, preview only
   - `cell-c` - text, about 2 word(s). Sample: Sample value
5. **faq-details** v6 - Zero-JavaScript FAQ accordion on native details/summary - hairline rows, a plus that turns into a cross when the row opens.
   - `section-title` - text, about 3 word(s). Sample: Sample questions heading
   - `question` - text, about 11 word(s). Sample: A sample question, phrased the way a visitor would ask it?
   - `answer` - markup, about 22 words of copy; its sample shape is printed in page.html's banner
6. **cta-band** v5 - Plain full-bleed closing call to action on a choice of four grounds - a centred claim, one supporting line and one control; the ordinary page finale, and the section most pages on a brand have in common.
   - `cta-band-id` - attribute (aria-labelledby/id): a page-unique id drawn from the heading
   - `eyebrow` - text, about 2 word(s). Sample: Sample eyebrow
   - `title` - text, about 5 word(s). Sample: Sample closing headline for preview
   - `lead` - text, about 9 word(s). Sample: One supporting sentence, used only to render this preview.
7. **colophon** v1 - The site footer a page carries itself - the brand's footer menu, the four legal links and a copyright line, set small on a quiet ground; the platform fills every link, the page owns the markup.
   - `copyright` - text, about 7 word(s). Sample: © 2026 Sample Brand. All rights reserved.

Also in `page.css`: **cta-assurance** v2, carried for hero-stated's `assurance` slot.

## What is still yours to decide

Copy, imagery and brand - never structure. The words in every slot are the brand's own; images are real material meeting each pattern's stated needs; the look comes from the brand's token values ([TOKENS.md](../../TOKENS.md)). The sections, their order and their markup are the composition - to change those, build from patterns directly instead.
