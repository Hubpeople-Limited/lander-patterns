# homepage-photo-editorial@1

Generated by `ci/compose.py` from `ci/page-recipes.json` - never hand-edit; change the source and regenerate.

A **homepage** page shell. The editorial photography homepage: the claim held above a wide image band rather than over it, alternating image-and-copy rows carrying the argument, and a photographic close. The image-led variant for a brand whose register is considered rather than cinematic.

Fetch `page.html` and `page.css`, write real copy into the slots, duplicate whatever block a pattern's comments say to duplicate, strip every comment, and keep each `{{ }}` token exactly as found - the platform fills those.

## Wiring

The brand's stylesheet loads first: link the brand's own `global.css` - its token values, per [TOKENS.md](../../TOKENS.md) - ahead of `page.css`, or merge `page.css` into it unchanged. `page.css` carries only the patterns' rules and reads every colour, radius and font from the brand's tokens.

The page carries its own site footer: this shell closes with the `colophon` pattern, and the platform fills its legal links and footer menu at serve time - keep every `{{ }}` token in it exactly as found. A site header is likewise a pattern a page carries (`masthead-nav` in this library); this composition ships without one.

This shell needs photography. An image slot takes real material - or, while the brand's photography is still coming, one of the library's stand-ins from `lib/placeholders/` (its README has the shapes and the rules): copied into the brand's own `site/images/`, named to the partner as a placeholder out loud, and recorded in the brand log as an image still owed. Never a placeholder for a person.

## Sections, in order

1. **hero-centred** v6 - Opener with the claim centred above a wide cropped photograph in its own band - the one hero that never lays a word over an image, so no scrim has to carry a ratio.
   - `headline` - text, about 4 word(s). Sample: Sample headline for preview
   - `subhead` - text, about 9 word(s). Sample: One supporting sentence, used only to render this preview.
   - `hero-image` - attribute (src): a path to real material, or a lib/placeholders/ stand-in while the brand's photography is still coming - said out loud, never silently
   - `hero-image-srcset` - attribute (srcset): a short label, about 4 word(s)
   - `hero-image-alt` - attribute (alt): what the image shows, in words
2. **zigzag-rows** v9 - Alternating image-and-copy rows down the middle of a page - the image side swaps row to row through an explicit modifier class, and only the image ever moves.
   - `section-id` - attribute (aria-labelledby/id): a page-unique id drawn from the heading
   - `section-title` - text, about 3 word(s). Sample: Sample section title
   - `row-1-image` - attribute (src): a path to real material, or a lib/placeholders/ stand-in while the brand's photography is still coming - said out loud, never silently
   - `row-1-image-alt` - attribute (alt): what the image shows, in words
   - `row-1-heading` - text, about 6 word(s). Sample: Sample heading for the first row
   - `row-1-body` - text, about 14 word(s). Sample: One or two sentences of sample body copy, used only to ...
   - `row-1-link-url` - attribute (href): a short label, about 1 word(s)
   - `row-1-link-text` - text, about 3 word(s). Sample: Sample link text
   - `row-2-image` - attribute (src): a path to real material, or a lib/placeholders/ stand-in while the brand's photography is still coming - said out loud, never silently
   - `row-2-image-alt` - attribute (alt): what the image shows, in words
   - `row-2-heading` - text, about 6 word(s). Sample: Sample heading for the mirrored row
   - `row-2-body` - text, about 14 word(s). Sample: The second sample row, which mirrors the first so the p...
   - `row-2-link-url` - attribute (href): a short label, about 1 word(s)
   - `row-2-link-text` - text, about 3 word(s). Sample: Sample link text
3. **faq-details** v6 - Zero-JavaScript FAQ accordion on native details/summary - hairline rows, a plus that turns into a cross when the row opens.
   - `section-title` - text, about 3 word(s). Sample: Sample questions heading
   - `question` - text, about 11 word(s). Sample: A sample question, phrased the way a visitor would ask it?
   - `answer` - markup, about 22 words of copy; its sample shape is printed in page.html's banner
4. **cta-image** v4 - Full-bleed photographic closing call to action - one landscape image, a scrim carrying the contrast, and a centred claim with both platform controls over it.
   - `cta-image-id` - attribute (aria-labelledby/id): a page-unique id drawn from the heading
   - `background-image` - attribute (src): a path to real material, or a lib/placeholders/ stand-in while the brand's photography is still coming - said out loud, never silently
   - `background-image-srcset` - attribute (srcset): a short label, about 4 word(s)
   - `background-image-alt` - attribute (alt): what the image shows, in words
   - `eyebrow` - text, about 2 word(s). Sample: Sample eyebrow
   - `title` - text, about 5 word(s). Sample: Sample closing headline for preview
   - `lead` - text, about 9 word(s). Sample: One supporting sentence, used only to render this preview.
5. **colophon** v1 - The site footer a page carries itself - the brand's footer menu, the four legal links and a copyright line, set small on a quiet ground; the platform fills every link, the page owns the markup.
   - `copyright` - text, about 7 word(s). Sample: © 2026 Sample Brand. All rights reserved.

## What is still yours to decide

Copy, imagery and brand - never structure. The words in every slot are the brand's own; images are real material meeting each pattern's stated needs; the look comes from the brand's token values ([TOKENS.md](../../TOKENS.md)). The sections, their order and their markup are the composition - to change those, build from patterns directly instead.
