{
  "_why": [
    "Real pages, as compositions of patterns, that must stay valid.",
    "",
    "Every other fixture in this repo is a single pattern, which is why a whole",
    "class of defect reached a live site: a pattern is correct on its own and",
    "wrong beside its neighbours. These recipes are the smallest thing that can",
    "hold the library to a page. Changing a pattern so one of them fails is not",
    "forbidden - it is a decision, and this file is where it has to be made",
    "deliberately rather than discovered by a partner.",
    "",
    "Each is a page somebody would plausibly build. Add one whenever a real",
    "build finds a composition worth protecting; do not add every permutation,",
    "because a fixture nobody understands is one that gets deleted the first",
    "time it fails.",
    "",
    "A recipe carrying a `composition` block additionally ships as a",
    "pre-assembled page shell under compositions/<name>@<version>/, generated",
    "by ci/compose.py. The block lives HERE, on the recipe, rather than in a",
    "sibling file, so there is exactly one statement of which patterns make a",
    "page: the fixture that must stay valid and the shell agents fetch are the",
    "same list, and neither can drift from the other. Bump `version` when the",
    "pattern list or its order changes - the folder name is the contract a",
    "consumer pinned. Everything else about a shell (slot names, guidance,",
    "pattern versions) is derived from the patterns at generation time and",
    "belongs to no file but theirs."
  ],
  "recipes": [
    {
      "page": "homepage",
      "patterns": ["hero-overlay", "stats-band", "cta-band"],
      "why": "The photo-led homepage built in round 23. A full-viewport opener is the case the fold check exists for."
    },
    {
      "page": "homepage",
      "patterns": ["masthead-nav:ground=soft", "hero-stated:ground=plain", "cta-band"],
      "why": "A page with its site header on it, which no other recipe has. masthead-nav is a component rather than a section on purpose: the fold check judges the first SECTION, so typing the header as one would quietly take every hero beneath it out of that check. This recipe is what notices."
    },
    {
      "page": "homepage",
      "patterns": ["hero-split", "steps-plain", "faq-details", "cta-band", "colophon"],
      "why": "Opener, how it works, the questions, the ask - the ordinary conversion homepage.",
      "composition": { "name": "homepage-conversion", "version": 2 }
    },
    {
      "page": "homepage",
      "patterns": ["hero-stated:ground=deep", "heading-block", "benefit-tiles", "cta-band", "colophon"],
      "why": "The homepage for a brand with no photography. heading-block is not decoration here: benefit-tiles opens at h3 and says so in its own markup, so without an h2 above it the page skips a heading level.",
      "composition": { "name": "homepage-no-photography", "version": 2 }
    },
    {
      "page": "pricing",
      "patterns": ["hero-stated:ground=plain", "pricing-tiers", "faq-details", "cta-band", "colophon"],
      "why": "State the offer, show the tiers, answer the objection, ask.",
      "composition": { "name": "pricing", "version": 2 }
    },
    {
      "page": "safety",
      "patterns": ["hero-stated:ground=brand", "safety-protections", "cta-band", "colophon"],
      "why": "On a dating brand this is a conversion page in disguise, so it closes like one.",
      "composition": { "name": "safety", "version": 2 }
    },
    {
      "page": "article",
      "patterns": ["article-masthead", "prose-column"],
      "why": "The whole of an article: its header and its body."
    },
    {
      "page": "landing",
      "patterns": ["hero-squeeze"],
      "why": "A one-screen landing page. Nothing below the fold, so the fold is the entire page."
    }
  ]
}
